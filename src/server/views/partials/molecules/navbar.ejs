<header>
 <% if (session) { %>
  <h3>Witaj <%= session.login %></h3>
 <% }%>
 <nav>
  <% if(session) { %>
  <a href="/profile"><button class="btn btn-profile">Profil</button></a>
  <a href="/login"><button class="btn btn-logout">Wyloguj</button></a>
  <% } else { %>
  <a href="/register"><button class="btn btn-register">Rejestracja</button></a>
  <a href="/login"><button class="btn btn-login">Logowanie</button></a>
  <% }%>
 </nav>
 </header>
 <div class="vs" >
  <span></span>
 </div>